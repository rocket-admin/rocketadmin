version: '2'
services:
  rocketadmin-agent:
    image: rocketadmin/rocketadmin-agent
    ports:
      - 7007:7007
    env_file: .config.env
    volumes:
      - ./dist:/app/dist
      - ./src:/app/src
    command: [ "yarn", "start:dev" ]
