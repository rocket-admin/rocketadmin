import { UrlValidatorDirective } from './url-validator.directive';

describe('UrlValidatorDirectiveTsDirective', () => {
  it('should create an instance', () => {
    const directive = new UrlValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
