import type { Meta, StoryObj } from '@storybook/angular';
import { PlaceholderRecordViewComponent } from './placeholder-record-view.component';

const meta: Meta<PlaceholderRecordViewComponent> = {
	title: 'Skeletons/PlaceholderRecordView',
	component: PlaceholderRecordViewComponent,
};

export default meta;
type Story = StoryObj<PlaceholderRecordViewComponent>;

export const Default: Story = {};
