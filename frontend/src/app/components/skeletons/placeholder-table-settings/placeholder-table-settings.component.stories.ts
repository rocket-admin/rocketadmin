import type { Meta, StoryObj } from '@storybook/angular';
import { PlaceholderTableSettingsComponent } from './placeholder-table-settings.component';

const meta: Meta<PlaceholderTableSettingsComponent> = {
	title: 'Skeletons/PlaceholderTableSettings',
	component: PlaceholderTableSettingsComponent,
};

export default meta;
type Story = StoryObj<PlaceholderTableSettingsComponent>;

export const Default: Story = {};
